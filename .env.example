# Copy to .env and fill in your own local Postgres credentials.
# Special characters in the password must be percent-encoded (@ -> %40, ! -> %21).
DATABASE_URL=postgresql+psycopg://postgres:yourpassword@localhost:5432/bank
